أريدك أن تقوم بتحديث نظام إدارة مفاتيح Gemini في المشروع بالكامل بحيث:

يدعم حتى 50 مفتاح API.

يوزّع الاستهلاك بينهم بشكل عادل وذكي.

يقلّل استهلاك الكوتا اليومية قدر الإمكان.

يدعم تحويل الفيديو حتى ساعتين عند الحاجة.

يوفّر لوحة تحكم إدارية (Dashboard) خاصة بي فقط لمراقبة حالة المفاتيح والاستهلاك، محمية بنظام تسجيل دخول من خلال قاعدة البيانات الموجودة.

ويطبع في الـ logs المسار (URL path) الخاص بلوحة التحكم الإدارية حتى أستطيع الوصول لها بسهولة.

مع جعل لوحة التحكم نفسها احترافية وليست مجرد جدول عادي (بها رسومات بسيطة، فلاتر، وإمكانية تعطيل/تفعيل المفاتيح).

نفّذ المطلوب بالتفصيل التالي، بدون تغيير وظائف الأدوات أو كسر أي Route موجود:

1️⃣ دعم حتى 50 مفتاح Gemini API ديناميكيًا

اجعل الكود يقرأ مفاتيح Gemini من متغيرات البيئة بالشكل التالي:

GEMINI_KEY_1

GEMINI_KEY_2

…

GEMINI_KEY_50

اجمع جميع المفاتيح غير الفارغة في قائمة واحدة، بدون الحاجة لتعديل الكود عند إضافة مفتاح جديد.

تجاهل المتغيرات الفارغة أو غير المُعرّفة.

2️⃣ نظام توزيع ذكي بين المفاتيح (Round-Robin + Cooldown)

استخدم نظام round-robin للتنقل بين المفاتيح، بحيث يتم استخدامها بالتساوي قدر الإمكان.

عند حدوث خطأ من نوع:

429

quota exceeded

rate limit
قم بوضع المفتاح في حالة cooldown لمدة ساعة (أو مدة مناسبة) ولا تستخدمه خلال هذه الفترة.

لكل طلب من المستخدم:

جرّب مفتاحًا واحدًا فقط في البداية.

إذا واجه هذا المفتاح خطأ quota/rate → جرّب مفتاحًا آخر واحد فقط كـ retry.

إذا فشل المفتاح الثاني أيضًا → أوقف المحاولة وأعد للمستخدم رسالة واضحة بأن الخدمة مشغولة أو تم بلوغ حد الاستخدام.

لا تقم بالدوران على كل المفاتيح الخمسين لطلب واحد؛ الحد الأقصى مفتاحان لكل طلب.

3️⃣ منع الاستدعاءات الخفية وغير الضرورية لـ Gemini

تأكد من عدم وجود استدعاءات لـ Gemini يتم تنفيذها:

بمجرد فتح صفحة.

أو عند تحميل الواجهة فقط لمجرد “التجربة”.

أو في أي loop/cron داخل السيرفر.

يجب أن يتم استدعاء Gemini فقط عند تنفيذ فعل حقيقي من المستخدم (ضغط زر، تحويل، تحليل، إلخ).

4️⃣ دعم تحويل الفيديو حتى ساعتين مع حماية الاستهلاك

اجعل نظام التحويل من فيديو إلى نص يدعم فيديو بطول يصل إلى 120 دقيقة (ساعتين) كحد أقصى.

عند استقبال فيديو طويل:

استخدم إعدادات تقلل استهلاك الكوتا قدر الإمكان:

ضغط الصوت/الفيديو قبل الإرسال إن أمكن.

استخدام نموذج أخف (مثل flash) ما لم تكن هناك حاجة ملحّة لنموذج أعلى.

إذا تجاوز الفيديو ساعتين:

لا تستدعِ Gemini، بل أعد للمستخدم رسالة أن الحد الأقصى المسموح هو ساعتان.

تأكد أن هذا التعديل لا يعطل الأداة، فقط يضيف حدًا منطقيًا.

5️⃣ إضافة حدود حماية خفيفة لمنع إساءة الاستخدام

أضف طبقة حماية (Rate Limiting بسيطة) بدون تعطيل المستخدم الطبيعي:

حد أقصى لعدد طلبات Gemini من نفس الجلسة أو نفس IP خلال فترة زمنية (مثلاً: عدد معقول من الطلبات كل 15–30 دقيقة).

حد أقصى منطقي لعدد عمليات تحويل الفيديو الطويل (قريبة من ساعتين) لكل مستخدم خلال فترة معينة.

عند تجاوز هذه الحدود:

أعد للمستخدم رسالة تنظيمية ودية (مثل:
“لقد وصلت إلى الحد الأقصى من العمليات في الفترة الحالية، يرجى الانتظار قليلاً قبل المحاولة مرة أخرى”)

ولا تحاول استدعاء Gemini في هذه الحالة.

6️⃣ Logging واضح لمراقبة الاستهلاك والمفاتيح

أضف نظام تسجيل (logging) واضح داخل الطبقة المركزية لاستدعاء Gemini بحيث يسجّل:

رقم المفتاح المستخدم (index أو رقم ترتيب، بدون طباعة قيمته الفعلية).

نوع العملية:

نص فقط

رؤية / صورة

تحويل صوت

تحويل فيديو

طول المحتوى التقريبي (مثل مدة الصوت/الفيديو إن أمكن).

هل هذه المحاولة الأولى أم retry.

نوع الخطأ إن حدث (429, quota, rate, 400, 403…).

لا تطبع قيمة المفتاح نفسه في أي log.

7️⃣ سلوك واضح عند استنفاد جميع المفاتيح

إذا كانت كل المفاتيح في حالة استنفاد أو Cooldown:

لا تدع التطبيق ينهار باستثناء داخلي.

بدلاً من ذلك:

أعد للمستخدم رسالة واضحة بالعربي مثل:
“تم استهلاك الحد المتاح من خدمة الذكاء الاصطناعي مؤقتًا. الرجاء المحاولة لاحقًا.”

دع الأدوات الأخرى غير المعتمدة على Gemini (قص، تحميل، تحويل ملفات محليًا، إلخ) تعمل بشكل طبيعي.

8️⃣ عدم إفساد أي أداة حاليّة

لا تغيّر الـ routes الحالية.

لا تغيّر واجهة الـ API أو شكل الاستجابات للمستخدم إلا في حالات الخطأ المتعلقة بالحدود والكوتا.

تأكد أن:

أداة تحويل الصوت إلى نص تعمل كما هي ضمن الحدود.

أداة تحويل الفيديو إلى نص تعمل حتى ساعتين، وترفض ما هو أكثر برسالة واضحة.

باقي الأدوات تظل تعمل بدون انقطاع.

9️⃣ إنشاء لوحة تحكم إدارية (Dashboard) لمراقبة المفاتيح والاستهلاك – محمية بحساب خاص

بما أن المشروع مرتبط بقاعدة بيانات، أريد لوحة تحكم إدارية سرّية خاصة بي فقط، تعرض حالة مفاتيح Gemini واستهلاكها، مع حماية عبر تسجيل دخول.

نفّذ التالي:

إنشاء صفحة Dashboard إدارية:

أنشئ Route وصفحة HTML مخصصة للوحة تحكم إدارية للمفاتيح (مثلاً /admin/keys أو مسار مشابه).

هذه الصفحة لا يجب أن تظهر في أي قائمة عامة ولا ترتبط بأي رابط للمستخدم العادي.

حماية الصفحة بنظام تسجيل دخول (Authentication):

استخدم النظام الحالي المرتبط بقاعدة البيانات (جدول المستخدمين) لإنشاء حساب Admin واحد على الأقل (أو استخدام حساب موجود وتعيينه كـ admin).

اجعل هذه الصفحة لا تُفتح إلا إذا كان المستخدم:

مسجّل الدخول.

وله صلاحية Admin (حقل role أو is_admin أو ما شابه ذلك).

إذا حاول مستخدم غير مصرح له الوصول إلى هذه الصفحة:

أعد توجيهه لصفحة تسجيل الدخول أو صفحة خطأ صلاحيات بسيطة.

المعلومات الأساسية التي يجب أن تظهر في لوحة تحكم المفاتيح:

عدد المفاتيح المُحمّلة حاليًا.

قائمة بالمفاتيح (بدون إظهار القيمة النصية للمفتاح) تتضمن لكل مفتاح:

رقم التعريف (مثلاً: Key #1, Key #2 … أو اسم المتغير GEMINI_KEY_5).

الحالة الحالية:

Active (يُستخدم)

In cooldown (منهك مؤقتًا بسبب quota)

Disabled/Empty (غير مُعرّف في البيئة)

آخر وقت تم استخدام هذا المفتاح فيه.

عدد الطلبات التي استخدمت هذا المفتاح تقريبًا (عداد usage).

عدد المرات التي تم فيها وضعه في حالة Cooldown.

ملخّص أعلى الصفحة يعرض:

إجمالي الطلبات لليوم الحالي (أو آخر 24 ساعة).

إجمالي الطلبات الصوتية / الفيديو / النصية.

عدد الأخطاء من نوع 429 خلال اليوم.

مصدر بيانات الـ Dashboard:

استخدم الـ logging أو counters التي أضفتها في الطبقة المركزية لاستدعاء Gemini لتجميع البيانات وعرضها في الـ Dashboard.

يمكن تخزين هذه البيانات:

إما في الذاكرة (in-memory) إذا كان ذلك بسيطًا.

أو في جدول خاص في قاعدة البيانات (مثلاً: جدول اسمه gemini_usage) إذا أردت حفظ السجل على المدى الطويل.

🔟 جعل لوحة التحكم أكثر احترافية (Advanced Dashboard Features)

أريد لوحة التحكم أن تكون احترافية وسهلة الفهم، ليست مجرد جدول خام، لذلك نفّذ الآتي:

واجهة مستخدم مرتبة (UI):

استخدم تصميم داكن (Dark Mode) يتماشى مع شكل ARKAN AI.

اجعل أعلى الصفحة قسم إحصائيات سريعة (Summary Cards) يعرض:

إجمالي عدد المفاتيح.

عدد المفاتيح النشطة حاليًا.

عدد المفاتيح في Cooldown.

إجمالي الطلبات لليوم الحالي.

عدد طلبات الصوت/الفيديو/النص (مجمعة).

رسم بياني بسيط (Charts):

أضف رسم بياني بسيط (يمكن أن يكون Line chart أو Bar chart) يعرض:

عدد الطلبات لكل ساعة خلال آخر 24 ساعة
أو

توزيع أنواع الطلبات (نص، صوت، فيديو) في اليوم.

الهدف ليس تعقيد الرسوم، بل إعطاء نظرة سريعة عن الاستهلاك.

فلاتر وترتيب:

أضف إمكانية:

ترتيب قائمة المفاتيح حسب:

الأكثر استخدامًا

الأكثر دخولاً في Cooldown

آخر استخدام

تصفية المفاتيح حسب الحالة:

Active

In Cooldown

Disabled/Empty

إمكانية تعطيل/تفعيل المفتاح يدويًا من الواجهة:

أضف زر لكل مفتاح في الجدول يسمح لي بأن:

أعطّل المفتاح يدويًا (Disable) حتى لو لم يكن في Cooldown.

أعيد تفعيله (Enable) إذا كان معطلاً يدويًا.

يجب أن يؤثر هذا على منطق اختيار المفاتيح:

المفاتيح المعطلة يدويًا لا تُستخدم في التوزيع حتى أعيد تفعيلها.

احفظ حالة التعطيل/التفعيل في قاعدة البيانات أو في بنية ثابتة تُحمّل عند بدء التطبيق، وليس فقط في الذاكرة المؤقتة.

عرض تحذيرات:

إذا تجاوز عدد الأخطاء من نوع 429 حدًا معينًا خلال اليوم:

اعرض تحذيرًا في أعلى لوحة التحكم، مثل:
“تحذير: عدد كبير من أخطاء quota اليوم، قد تحتاج إلى زيادة عدد المفاتيح أو ضبط الحدود.”

1️⃣1️⃣ عدم ظهور لوحة التحكم للمستخدم العادي

لا تضف أي رابط للوحة التحكم في واجهة المستخدم العادية.

الوصول للـ Dashboard يجب أن يكون فقط عن طريق:

تسجيل الدخول بحساب Admin.

كتابة رابط لوحة التحكم يدويًا في المتصفح.

1️⃣2️⃣ طباعة مسار لوحة التحكم في الـ Logs لي فقط

بعد إنشاء Route الخاص بلوحة التحكم الإدارية، أريدك أن:

تطبع في الـ server log (عند تشغيل التطبيق أو عند أول تهيئة) رسالة واضحة تحتوي على مسار لوحة التحكم الإدارية، مثلاً:

Admin dashboard for Gemini keys is available at: /admin/keys (requires admin login)

أو المسار الفعلي الذي استخدمته.

1️⃣3️⃣ اختبار شامل بعد التعديلات

بعد تنفيذ جميع النقاط:

اختبر الأدوات كما كان من قبل لتتأكد أن كل شيء يعمل طبيعي.

اختبر:

عدة طلبات متتالية لترى كيف يتم تدوير المفاتيح.

طلب فيديو طويل (قريب من ساعتين) للتأكد أن الأداة تعمل ضمن الحد الجديد.

طلب فيديو أطول من ساعتين للتأكد من ظهور رسالة الحد الأقصى.

سجّل الدخول بحساب Admin وادخل إلى صفحة الـ Dashboard:

تأكد أن واجهة لوحة التحكم مرتبة وتعرض الإحصائيات بشكل واضح.

تأكد أن الفلاتر، الفرز، والرسم البياني يعملون.

تأكد أن تعطيل/تفعيل المفاتيح يدويًا يؤثر فعليًا على استخدامها في التوزيع.

راقب الـ logs لتتأكد أن مسار لوحة التحكم الإدارية طُبع بوضوح كما طُلب.

نفّذ كل ما سبق مباشرة داخل المشروع، ولا تعرض الأكواد في الرد، فقط عدّل ملفات المشروع نفسها بما يتوافق مع هذه التعليمات.